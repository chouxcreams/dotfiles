name: lint

on:
  push:
    branches: [ main ]

jobs:
  lint-check:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install required packages
      run: brew install shellcheck

    - name: Check init.sh
      run: shellcheck ${GITHUB_WORKSPACE}/scripts/*
