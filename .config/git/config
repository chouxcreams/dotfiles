[include]
    path = local.conf

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[delta]
    navigate = true
    side-by-side = true

[interactive]
    diffFilter = delta --color-only

[alias]
    sc = switch -c
    po = push --set-upstream origin HEAD
